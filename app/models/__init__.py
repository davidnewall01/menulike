"""Model package.

Empty for now — content models (site/identity + the menu tree) land in the next
prompt. Alembic imports this package so every model is registered on
`Base.metadata` before migrations run.
"""
